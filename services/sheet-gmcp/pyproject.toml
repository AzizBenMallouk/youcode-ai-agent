[project]
name = "sheet-gmcp"
version = "0.1.0"
description = "Google Sheets MCP Server"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.2.0,<2.0.0",
    "gspread>=6.1.2",
    "pydantic>=2.9.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
