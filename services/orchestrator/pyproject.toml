[project]
name = "orchestrator-service"
version = "0.1.0"
description = "YouCode AI orchestrator — supervisor, guardrail and MCP agent client"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "httpx",
    "langchain-google-genai",
    "langchain-ollama",
    "langchain-xai",
    "langchain",
    "langchain-community",
    "langgraph",
    "langgraph-checkpoint-postgres",
    "psycopg[binary]",
    "pydantic",
    "pydantic-settings",
    "qdrant-client",
    "langchain-qdrant",
    "better-profanity",
    "mcp>=1.2.0,<2.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
